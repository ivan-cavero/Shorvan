{
  "name": "shorvan",
  "author": "Ivan Cavero",
  "description": "Shorvan is a URL shortener that helps you shorten links. Developed by Ivan Cavero",
  "license": "MIT",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "git checkout master && git merge develop && npm version patch -m 'Release: %s' && git push origin master && git checkout develop && git push origin develop",
    "hotfix": "git checkout master && git merge develop && npm version patch -m 'Hotfix: %s' && git push origin master"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "@astrojs/vercel": "^7.5.2",
    "@midudev/tailwind-animations": "^0.0.7",
    "astro": "^4.5.16",
    "tailwindcss": "^3.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.4"
  }
}